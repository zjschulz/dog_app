<h2>Volunteer List</h2>

<%= link_to "Create New Volunteer", volunteer_path %>

<% @volunteers.each do |volunteer| %>
    <h3><%= link_to volunteer.name, volunteer_walks_path(volunteer) %></h3>
    <p><%= time_conversion(volunteer.time_method)%></p>
<% end %>